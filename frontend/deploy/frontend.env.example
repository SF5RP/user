# Production: copy to:
#   /srv/auth-service/shared/frontend/env/frontend.env
# and fill in the values (chmod 600).
#
# NEXT_PUBLIC_* variables are baked into the standalone build at build time.
# This file is only for runtime variables read by Node/systemd.

NODE_ENV=production
PORT=3000
