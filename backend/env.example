# Discord OAuth2 Configuration
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_CLIENT_SECRET=your_discord_client_secret_here
DISCORD_REDIRECT_URI=http://localhost:8080/api/callback

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
# Должно совпадать с тем, что отправляет казино в return_to:
# window.location.origin + "/auth/callback"
ALLOWED_RETURN_URLS=http://localhost:3000/auth/callback

# Security
JWT_SECRET=your_jwt_secret_key_here_make_it_very_long_and_secure

# Server Configuration
SERVER_PORT=8080
ENVIRONMENT=production

# Admin Discord IDs (comma-separated)
ADMIN_DISCORD_IDS=

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=auth
DB_PASSWORD=your_secure_database_password_here
DB_NAME=authdb
DATABASE_URL=postgres://auth:your_secure_database_password_here@localhost:5432/authdb?sslmode=disable
